export function listingId(item) {
  return item.listing_id || item.id;
}

export const routes = {
  browse: "/",
  rentals: "/rentals",
  projects: "/projects",
  insights: "/insights",
  saved: "/saved",
};

export const assignedLocality = "Andheri West";
